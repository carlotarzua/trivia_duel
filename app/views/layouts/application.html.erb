<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Trivia" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/td1.png" type="image/png">
    <link rel="icon" href="/td1.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/td1at.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag 'td1.ico' %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
